<link href="/styles/roster.css" type="text/css" rel="stylesheet" />


<h1 class="page-title">ΟΜΑΔΑ</h1>


    <div class="row">
        <h3>Τερματοφύλακες</h3>
        <div class="players">
            <% players.forEach(player => { %>
            <%if(player.position === 'goalkeeper'){ %>
            <%- include('player-card.ejs',{player:player}) %>
            <!-- passing the player object to the play-card -->
            <% } %>
            <% }) %>
        </div>
    </div>

    <div class="row">
        <h3>Αμυντικοί</h3>
        <div class="players">
            <% players.forEach(player => { %>
            <%if(player.position === 'defender'){ %>
            <%- include('player-card.ejs',{player:player}) %>
            <% } %>
            <% }) %>
        </div>
    </div>

    <div class="row">
        <h3>Μέσοι</h3>
        <div class="players">
            <% players.forEach(player => { %>
            <%if(player.position === 'midfielder'){ %>
            <%- include('player-card.ejs',{player:player}) %>
            <!-- passing the player object to the play-card -->
            <% } %>
            <% }) %>
        </div>
    </div>

    <div class="row">
        <h3>Επιθετικοί</h3>
        <div class="players">
            <% players.forEach(player => { %>
            <%if(player.position === 'forward'){ %>
            <%- include('player-card.ejs',{player:player}) %>
            <!-- passing the player object to the play-card -->
            <% } %>
            <% }) %>
        </div>
    </div>
